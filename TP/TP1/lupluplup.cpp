/*#include <iostream>

using namespace std;

int main(){
    int x, positif, negatif;
    cout << "masukan nilai: ";
    cin >> x;
    positif = 0;
    negatif = 0;
    while (x != 0){
        if (x > 0) {
            positif++;
        }
        if (x < 0) {
            ++negatif;
        }
        cin >> x;
    }
    if (positif > negatif) {
        cout << "positif" << endl;
    }else if (negatif > positif){
        cout << "negatif" << endl;
    }else {
        cout << "imbang" << endl;
    }
    cout << negatif << endl;
    cout << positif << endl;
    return 0;
}
*/
