#include "sll.h"

int main()
{
    float x , y;
    cin >> x;
    cin >> y;
    bool apa = y < x;
    cout << apa << endl;
    /*List L1, L2;
    address P;
    infotype X;
    float rataRating;
    int i, rating;

    createList(L1);
    createList(L2);

    /*for (i = 1; i <= 3; i++){
        cout << "Nama Penumpang : ";
        cin >> X.namaPenumpang;
        cout << "Nama Driver : ";
        cin >> X.namaDriver;
        cout << "Rating Driver : ";
        cin >> X.rating;
        P = createNewElemen(X);
        insertLast(L1, P);
    }

     for (i = 1; i <= 3; i++){
        cout << "Nama Penumpang : ";
        cin >> X.namaPenumpang;
        cout << "Nama Driver : ";
        cin >> X.namaDriver;
        cout << "Rating Driver : ";
        cin >> X.rating;
        P = createNewElemen(X);
        insertFirst(L1, P);
    }

    cout << endl;
    showList(L1);
    resetList(L1);
    cout << endl;
    showList(L1);

    for (i = 1; i <= 6; i++){
        cout << "Nama Penumpang : ";
        cin >> X.namaPenumpang;
        cout << "Nama Driver : ";
        cin >> X.namaDriver;
        cout << "Rating Driver : ";
        cin >> X.rating;
        P = createNewElemen(X);
        insertLast(L1, P);
    }
    cout << endl;
    showList(L1);

    for (i = 1; i <= 3; i++){
        deleteFirst(L1, P);
    }
    cout << endl;
    showList(L1);

    cout << "Rating after : ";
    cin >> rating;
    cout << "Nama Penumpang : ";
    cin >> X.namaPenumpang;
    cout << "Nama Driver : ";
    cin >> X.namaDriver;
    cout << "Rating Driver : ";
    cin >> X.rating;
    P = createNewElemen(X);
    insertAfterValue(L1, P, rating);
    cout << endl;
    showList(L1);

    cout << "Rating after : ";
    cin >> rating;
    deleteAfterValue(L1, P, rating);
    cout << endl;
    showList(L1);

    rataRating = rataRataRating(L1);
    cout << "Rata-Rata Rating : " << rataRating << endl;

     for (i = 1; i <= 6; i++){
        cout << "Nama Penumpang : ";
        cin >> X.namaPenumpang;
        cout << "Nama Driver : ";
        cin >> X.namaDriver;
        cout << "Rating Driver : ";
        cin >> X.rating;
        P = createNewElemen(X);
        insertLast(L1, P);
    }
    cout << endl;
    showList(L1);
    Sorting(L1);
    cout << endl;
    showList(L1);

    splitListSort(L1, L2, 6);
    cout << endl;
    showList(L1);
    cout << endl;
    showList(L2);*/
    return 0;
}
